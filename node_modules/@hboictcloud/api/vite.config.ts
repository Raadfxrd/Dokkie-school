import { defineConfig } from "vite";
import dts from "vite-plugin-dts";

export default defineConfig({
    build: {
        lib: {
            entry: "./src/index.ts",
            formats: ["es"],
            name: "HBO-ICT.Cloud API",
            fileName: "hboictcloud-api",
        },
        sourcemap: true,
    },
    plugins: [
        dts({
            rollupTypes: true,
        }),
    ],
});
